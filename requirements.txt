# Phase 1 — data pipeline
datasets>=2.18.0
pandas>=2.0.0
pyarrow>=14.0.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# Phase 3–4 — LLM (Groq)
groq>=0.9.0

# Phase 5 — UI
streamlit>=1.28.0

# Backend API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Testing
pytest>=7.4.0
