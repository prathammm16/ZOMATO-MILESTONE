# Slim install for Railway (API only — excludes Streamlit to save ~100MB RAM)
datasets>=2.18.0
pandas>=2.0.0
pyarrow>=14.0.0
pydantic>=2.5.0
python-dotenv>=1.0.0
groq>=0.9.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
